# 프로토타입 구현 메모 (2026-09-17, 최신 커밋 9f7d17e)

## 원본
Figma `2026.09-Nologin-Main` › 컴포넌트 `[MO] NoLogin Main-홈` (node 40007048:4435). 프레임 510 · 콘텐츠 470 · 좌우 20.

## 구현 방식
- index.html 01 탭 `.phone`(394) 안에 `.np` 루트 — width 510, padding 0 20, `zoom:.7725`
- 클래스 접두사 `np-` (리포트 `r-`, 프레임 `.phone` 과 분리)
- 상태 `NP` 메모리 객체, 부분 렌더. 리로드 = `npInit()`
- 이미지 54개: Figma MCP 자산 서버 fetch 차단 → Chrome 확장으로 자산 URL 직접 열어 zoom 캡처 → 마젠타 키잉 → PNG. `assets/figma/*.png` + base64 임베딩
- 폰트: Pretendard 통일 (HEO 지시)

## Figma 실측 대조 (d02bb84)
텍스트 폭을 Figma 노드 폭과 비교 → Pretendard 구간 ±2px 일치. 차이는 레이아웃 수치였음.

| 항목 | 수정 전 | 수정 후 | Figma |
|---|---|---|---|
| PlanCard | 257 | 244 | 244 |
| PhoneCard | 360 | 346 | 346 |
| PLANE 섹션 | 425 | 374 | 370 |
| 전체(타임딜 1펼침) | 6,516 | 4,893 | 4,872 |

주요 수정
- gap 구조를 Figma 그룹 단위로 재구성 (PlanCard 10/10/4, PhoneCard 10→20→16→6)
- PLANE 섹션 gap 0, 도트를 루트 형제로 이동(gap 30)
- 로고·검색·메뉴 정렬: space-between → 로고 flex-1 + 아이콘 인접
- 배너 텍스트 250px 고정 → 이미지 위치 복원
- **버그**: `.np-more` 가 `.np button` 리셋(특이도 0,0,1,1)에 덮여 배경·패딩 소실 → `.np .np-more` 로 보강
- tracking 을 Figma px 선언값으로 전면 적용

## 플리킹 (9f7d17e) — 배너 · 요금제 · 휴대폰
- `npFlick(el, getStep, getCount, onIdx)` 공통 헬퍼
- 마우스 드래그 + 속도 기반 관성 스냅(임계 0.35px/ms). 터치는 브라우저 기본 스크롤 유지(`pointerType==='mouse'` 만 가로챔)
- zoom 컨테이너 대응: 이동량을 `rect.width/clientWidth` 배율로 환산
- 레일 좌측 패딩 16px 을 인덱스↔scrollLeft 환산에 반영. 끝단은 마지막 인덱스로 고정
- 도트를 버튼으로 전환 → 클릭 이동, `:after` 로 히트영역 24/28px 확보
- 키보드 ← → Home End
- **함정 2건**
  1. `setPointerCapture` 사용 시 후속 `click` 이 레일로 리타겟되어 카드 탭이 동작하지 않음 → document 리스너로 대체
  2. 드래그 직후 클릭이 카드 상세로 새는 문제 → 이동량 6px 초과 시 캡처 단계에서 클릭 차단(320ms)

## 기본 상태 (HEO 지시)
- 퀵메뉴(홈 탭 4) 스크롤 시 상단 고정 — `.np-menuwrap` sticky, 510 풀블리드 흰 배경
- 타임딜 3건 전부 접힘 · 푸터 사업자 정보 닫힘
- 요금제·휴대폰 카드 각 5장

## 데이터
Figma 원문 1건씩(배너 · 타임딜 · 요금제 카드 · 휴대폰) + 나머지 샘플 표기

## 남은 확인 항목
- Noto Sans KR 구간을 Pretendard 로 대체 → 큰 숫자(0원 등) 폭이 Figma 대비 좁음. 지시대로 유지 중
- BUNDLE 카드: Figma 선언값 205px(우측 여백 발생) vs 사용자 export 는 꽉 참 → 꽉 참(flex:1)으로 구현
- 터치 타깃 44px 미달 다수 (Figma 수치 유지). 확대 여부 미결
- O-03(타임딜 데이터) · O-04(배너 자동 회전) · O-06(홈 외 탭)

## 휴대폰 탭 (2026-09-18)
원본 Figma `[MO] NoLogin Main-휴대폰` (node 40007048:4637). 프레임 510 · 카드 470×294.

구성 — TITLE COPY · Filter STEP2 는 Figma 에서 hidden → 미구현
- Filter STEP1: 전체 칩 + 제조사 ▾ + 휴대폰 상태 ▾ (gap 8)
- PhoneCard 5장 리스트 (gap 10 · 리스트 상단 여백 10)

PhoneCard 실측 (p20 · gap10 · r16 · border 1 #e8e8e8)
| 블록 | 수치 |
|---|---|
| 배지 | h26(테두리 1 포함) · gap 6 · r4 · 텍스트 #f9fafb |
| 이미지 | 130×130 · 패딩 10 |
| 정보 | gap 10 · 세대칩 41×30 r10 · 상품명 14/24 SB |
| 요금 2행 | 라벨 좌 · 금액 우 · 14/24 R |
| 가격 | h56 우측 정렬 · 취소선 14/24 · 「월 납부금액」 18/28 + 금액 24/36 #7c4dff |

배지 색 — 한정판매 #6355ff · 인기 #e60076 · S급 #444a62 · 신규 #2b7fff · 갓성비 #00c950. 폰트는 Figma 12 → 프로젝트 하한 14px.

렌더 실측 대조: 카드 470×294.5 (Figma 470×294) — ±2px 이내.

드롭다운 (HEO 지시)
- Figma 는 트리거만 존재, 옵션 목록 미정의 → 카드 데이터 파생 샘플 옵션으로 동작 구현
- 제조사 = 삼성 · 애플 / 휴대폰 상태 = 새제품 · 중고S급 · 미개통 전시폰. 두 조건 AND
- 각 목록 말미 「전체」 = 해당 축 해제. 상단 「전체」 칩 = 두 축 동시 해제
- 팝오버 외부 클릭 · 탭 전환 시 닫힘(`npMClose`, document 캡처 리스너)
- 옵션 값은 **샘플**. 실제 분류 체계 확정 시 `NP_MOPT` 교체

구현
- `#np-phonetab` · 클래스 `np-mfilter` `np-dd` `np-mcard`. 데이터 `NP_DATA.mtab`
- 상태 `NP.mk`(제조사) · `NP.ms`(휴대폰 상태). `npInit()` 에서 null 복귀
- `npTab('phone')` → `npPhoneTab()`. 미정의 탭 안내는 유픽폰만 잔존
- 카드 이미지: Figma 자산 5종(image10~14) 미반출 → 홈 탭 `imgImage2` 단일 이미지 재사용. **기기별 이미지 미반영**

Figma 원문 불일치 1건
- 2번 카드(아이폰13 미니): 통신요금 28,500 + 할부금 17,270 = 45,770 이나 월 납부금액 28,500 표기. Figma 원문 유지, 합계 미보정

## 유픽폰 탭 (2026-09-18)
원본 Figma `[MO] NoLogin Main-유픽폰` (node 40007048:5390). 프레임 510 · 전체 높이 1779.

구성 — TITLE COPY 는 Figma 에서 hidden → 미구현
- HERO 466×375 (Figma 단일 이미지 image16)
- Filter STEP1: 칩 3 + 우측 정렬 버튼 (gap 8 · 상하 여백 30/20)
- UPickCard 5장 리스트 (470×192 · gap 10 · 리스트 상단 여백 10)

UPickCard 실측 (p20 · gap10 · r16 · border 1 #e8e8e8)
| 블록 | 수치 |
|---|---|
| 이미지 | 130×130 · 패딩 10 |
| 상품명 행 | 상품명 14/24 SB 좌 · 세대칩 41×30 r10 우 |
| 회원가 행 | 「유모바일 회원가」 14/24 R · 금액 우측 14/24 R |
| 납부금액 행 | 「월 납부금액(예상)」 14/24 R · 금액 우측 16/24 SB #635dff |
| 단서 | 우측 정렬 불릿 1행 14/24 SB #424242 |

렌더 실측 대조: 카드 470×192.2 · HERO 470×375 (Figma 470×192 · 466×375). HERO 폭은 기존 탭과 동일하게 470 적용.

HERO — Figma 단일 이미지, 자산 미반출 → HTML·CSS 재구성
- 그라데이션 + 중앙 글로우 · 인증 배지(육각 clip-path) · 타이틀 2행 · 서브 2행 · 3열(무약정 · 인증기기 · 특별 할인가) · 하단 단말 실루엣
- 아이콘 3종은 인라인 SVG 로 대체. **픽셀 동일성 아님** — 문구 · 레이아웃 · 높이(375)만 일치

필터 칩 — Figma 표기와 레이어명 불일치 1건
- 표기 「전체 · LTE · 애플」 / 레이어명 「전체 · 삼성 · 애플」 (요금제축 · 제조사축 혼재)
- 레이어명 기준 제조사 단일축으로 통일 → 「전체 · 삼성 · 애플」. 휴대폰 탭 제조사 필터와 기준 일치
- Figma 원문 카드 5건 전부 삼성 → 애플 칩은 빈 상태. 빈 상태 문구에 사유 명기

정렬 — Figma 는 트리거 「인기순」만 존재, 옵션 미정의 → 샘플 3종(인기순 · 낮은 가격순 · 높은 가격순). 인기순 = 원문 순서, 나머지는 월 납부금액 기준

구현
- `#np-upicktab` · 클래스 `np-uhero` `np-ufilter` `np-usort` `np-ucard`. 데이터 `NP_DATA.upick`
- 상태 `NP.uk`(제조사) · `NP.us`(정렬). `npInit()` 에서 null · '인기순' 복귀
- `npMClose()` 대상을 `#np .np-dd` 전체로 확대 → 휴대폰 · 유픽폰 드롭다운 공용
- 카드 이미지: 휴대폰 탭과 동일하게 `imgImage2` 단일 이미지 재사용. **기기별 이미지 미반영**

4개 탭 전건 구현 완료 → O-06(홈 외 탭) 해소. 미정의 탭 안내 블록(`#np-other`)은 미사용 상태로 잔존

## 리스트 샘플 증량 (2026-09-18)
휴대폰 · 유픽폰 각 10건 추가 → 각 15건 (Figma 원문 5 + 샘플 10). 샘플은 상품명 앞 「샘플 · 」 표기.

| 탭 | 전체 | 삼성 | 애플 | 새제품 | 중고S급 | 미개통 전시폰 |
|---|---|---|---|---|---|---|
| 휴대폰 | 15 | 10 | 5 | 8 | 4 | 3 |
| 유픽폰 | 15 | 10 | 5 | — | — | — |

- 샘플 10건은 통신요금 + 할부금 = 월 납부금액 정합. Figma 원문 2번 카드 불일치 1건은 원문 유지
- 유픽폰 애플 샘플 추가 → 애플 칩 빈 상태 해소. 빈 상태 사유 문구 제거
- 세대칩 폭 `width:41px` → `min-width:41px` (LTE 표기 오버플로 방지). Figma 5G 기준 렌더 동일

## 홈 링크 버튼 보완 (2026-09-18)
헬퍼 `npLink(to, label)` 도입 — 탭 대상은 실제 이동(`npTab`), 그 외는 범위 밖 표기로 분리.

| 대상 | 동작 |
|---|---|
| 최상단 배너 6장 | 툴팁 「링크된 페이지로 이동」 (HEO 지시). 탭 이동 없음 · 배너 `to` 필드 제거 |
| 배너 전체보기 | 툴팁 「프로모션 목록 레이어 팝업 오픈」 (HEO 지시). 탭 이동 없음 |
| 추천 요금제 전체보기 | 요금제 탭 이동 |
| 추천 휴대폰 전체보기 | 휴대폰 탭 이동 |
| 로그인 · 혜택 전체보기 · 결합 4 · 고객지원 4 · 사용후기 · APP | 「— 외부 페이지 (프로토 범위 밖)」 토스트로 통일 |
| 요금제 카드 (홈 추천 · 요금제 탭 · 타임딜 항목) | 툴팁 「요금제 상세 페이지로 이동」 |
| 휴대폰 카드 (홈 추천 · 휴대폰 탭) | 툴팁 「휴대폰 상세 페이지로 이동」 |
| 유픽폰 카드 | 툴팁 「유픽폰 상세 페이지로 이동」 |

- 배너 슬라이드에 `role="button"` `tabindex="0"` · `cursor:pointer` 부여. aria-label 에 배너명 추가
- `#np` 내 `role="button"` 요소 Enter · Space 활성화 리스너 추가 → 카드·배너 전체 키보드 조작 가능
- 드래그 후 클릭 누출 없음(기존 6px 임계 가드가 배너 레일에도 적용됨) — 검증 완료

## 로고 · 전체보기 보완 (2026-09-18)
- 상단 로고를 버튼으로 전환 → 홈 탭 이동. `aria-label="홈으로 이동"` · cursor pointer · 키보드 활성화
- 동일 탭에서 재선택 시 토스트 「<탭명> 탭 — 맨 위로」 (npTab 의 npTop 동작과 일치)
- 배너 영역은 탭 이동 없이 이동 대상만 툴팁 표기 — 배너 카드 「링크된 페이지로 이동」 · 전체보기 「프로모션 목록 레이어 팝업 오픈」
- 링크 동작 3종: `npLink(tab)` 실제 탭 이동(추천 요금제·휴대폰 전체보기 · 로고) / 툴팁 표기(배너) / `npLink('')` 범위 밖

## 툴팁 문구 확정 (2026-09-18, HEO 지시)
이동 대상을 툴팁 문구로 고정. 상품명 등 가변 텍스트 제거 — 동일 동작은 동일 문구.

| 선택 대상 | 툴팁 |
|---|---|
| 최상단 배너 카드 | 링크된 페이지로 이동 |
| 배너 전체보기 | 프로모션 목록 레이어 팝업 오픈 |
| 요금제 카드 전체 | 요금제 상세 페이지로 이동 |
| 휴대폰 카드 전체 | 휴대폰 상세 페이지로 이동 |
| 유픽폰 카드 | 유픽폰 상세 페이지로 이동 |
